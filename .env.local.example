# Local Development Environment Variables
# Copy this file to .env.local for local testing

# Debug level (0 = off, 1 = basic info, 2 = debug, 3 = verbose)
DEBUG_LEVEL=1

# Custom User-Agent header for requests (optional)
# USER_AGENT=my-translation-server/dev (local testing)

# Port to run the server on (for local server.js)
# PORT=1969

# Proxy configuration (optional)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# Text search via AWS Lambda (optional, leave empty/commented to disable)
# identifierSearchLambda=identifier-search-function-name
