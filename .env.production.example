# Vercel Production Environment Variables (Example)
# Copy this file to .env.production and configure for your deployment

# Debug level (0 = off, 1 = basic info, 2 = debug, 3 = verbose)
DEBUG_LEVEL=1

# Custom User-Agent header for requests
# It's recommended to customize this to identify your server
# USER_AGENT=my-custom-translation-server/2.0 (me@example.com)

# Proxy configuration (optional)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# TLS configuration for self-signed certificates (only if necessary)
# NODE_TLS_REJECT_UNAUTHORIZED=0

# Note: The identifierSearchLambda feature is not supported on Vercel
# If you need full-text search, consider using AWS Lambda instead
